Just an Exercise on PiAutoAGUI and Playwright. 
There is a very simple automation code that shows an how to open a notepad and send the keys to then open a browser and send the keys and also use the mouse to maximise the size of the screen to zoom
